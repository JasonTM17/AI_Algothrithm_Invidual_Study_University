services:
  - type: web
    name: eight-puzzle-search-lab
    env: python
    plan: free
    buildCommand: python -m pip install --upgrade pip && python -m pip install -r requirements.txt
    startCommand: python -m streamlit run streamlit_eight_puzzle_app.py --server.address=0.0.0.0 --server.port=$PORT
